# data-governance-gateway
PII detection and data-contract enforcement gateway for data pipelines: scans records for PII before they reach downstream consumers and validates them against versioned YAML/JSON-schema data contracts. Deployable as a standalone FastAPI service or sidecar container.
